export async function publishDueScheduledPosts(): Promise<void> {
  // The current home-post schema does not include scheduled publish metadata,
  // so there is nothing to publish here yet.
  return;
}
